# Proto

향후 실시간 전투 서버 도입 시 사용할 Protocol Buffers 명세를 둡니다.

현재 서버 통신은 HTTP/JSON을 사용하므로 Proto 명세는 작성되지 않았습니다. 실시간 전투 서버 또는 gRPC 도입이 확정될 때 `.proto` 파일과 버전 관리 규칙을 추가합니다.

현재 REST 계약은 [OpenAPI 디렉터리](../OpenAPI/README.md)에서 관리합니다.
